# CoDA 复现与消融实验记录 — ImageWoof

> 2026-07-25 ~ 07-27，单张 RTX 3090。
> 目标：复现论文 Table 2 中 ImageWoof / IPC=10 / ResNetAP-10 那一格（**39.2±0.7**），
> 并搞清楚复现不出来的原因。

---

## 0. 结论速览

1. **复现不出 39.2**，最好一次 35.0，差 4.2 个点。
2. **数据集划分不是原因**——fastai imagewoof2 那份跑出来 30.85，更低。
3. **CoDA 的 Distribution Discovery 模块没有可测收益**——换成它所批评的朴素 K-Means，
   G 打平（35.4 vs 35.0），选真实图还高 3 个点（32.4 vs 29.4）。
4. **真正起作用的是"引导目标的数量"而非"目标的正确性"**——目标从 10 个不同减到 3~5 个，
   G 从 35.0 崩到 24.4；而在 10 个不同目标的前提下，怎么选几乎不影响结果（33.6~35.4）。
5. **论文公式与代码不一致**：代码的引导项多了一个 σ_t 因子，`guideTPercent` 的语义也和注释里的
   PIS 公式对不上。

---

## 1. 环境与数据

### 两份 ImageWoof（关键）

"ImageWoof" 这个名字指两个不同的东西，论文通常不注明用的哪一份：

| | fastai imagewoof2 | ImageNet-1K 子集 |
|---|---|---|
| 来源 | Jeremy Howard 的 fastai 仓库，2019-12-06 起改为 70/30 划分 | IDC (ICML 2022) 起的蒸馏文献惯例，类别列表套完整 ImageNet |
| 训练 | 9,025 | 12,454 |
| 验证 | 3,929（96% 是 ImageNet 训练集的图） | 500（纯官方 val，50/类） |
| 本地路径 | `/media/lm/_dde_data/data/imagewoof` | `/media/lm/_dde_data/data/imagewoof_in1k` |

**两份的图片总数都是 12,954**，因为 fastai 就是把这 10 类的 ImageNet train(12,454) + val(500)
合并后重切。证据：fastai 的 `train/` 里能翻出 `ILSVRC2012_val_*.JPEG`。

本记录的主线用 ImageNet-1K 子集（软链构建，脚本 `scratchpad/build_woof.py`）；
fastai 那份单独跑了一轮对照（见 §4.5）。

### 模型与加速

SDXL base 1.0 + refiner 1.0，fp16，`model/SDXL-Refiner/`（12.4 GB）。

下载慢的真凶是 `hf_xet`（走独立的 `cas-server-*.xethub.hf.co`，代理下退化到几百 KB/s），
不是代理本身。已在 `~/.bashrc` 写入 `HF_HUB_DISABLE_XET=1` + hf-mirror 端点 + 国内镜像直连的
`no_proxy`，并提供 `hf_mirror` / `hf_official` 两个切换函数。

### 脚本

| 文件 | 用途 |
|---|---|
| `scripts/CoDA_woof.sh` | 主运行脚本，阶段开关与超参走环境变量 |
| `scripts/CoDA_woof_fastai.sh` | 同上，数据指向 fastai 那份 |
| `kmeans_baseline.py` | MGD³ 式朴素 K-Means 选点（消融用） |
| `quota_baseline.py` | Idea 3：不补齐，按簇大小分配生成配额 |
| `random_baseline.py` | 随机选点基线 |
| `select_variants.py` | Idea 1（空间池化）+ Idea 2（最远点采样） |

修过原脚本一处 bug：`train_save_dir` 不含数据来源标记，评 real 和评 gen 会互相覆盖。

---

## 2. 流水线与耗时（单卡 3090）

| 阶段 | 产物 | 耗时 |
|---|---|---|
| 1 特征提取 | VAE latent 缓存 13 GB | 38.5 min（每个数据集只需一次） |
| 2 聚类 | `real_images/` 集合 **R** | 4.5 min（CPU） |
| 3 生成 | `generated_images/` 集合 **G** | 14.3 min（8.5 s/张） |
| 4 评测 | ResNetAP-10, 256², 2000 ep | 6 min/次 |

复用特征缓存的话，一组完整消融约 **25 分钟**。

---

## 3. 固定超参

`IPC=10`, `size=256`, `n_neighbors=85`, `min_samples=3`, `sample_step=25`,
`DF=1.0`（全程 base，refiner 不参与）, `GTP=0.9`, `γ=0.05`, `cfg=5.0`,
评测 ResNetAP-10 / 2000 epochs / `repeat=1`（**单卡单种子，`±0.00` 不是真方差**）。

这组是仓库给 **ImageNet-A** 的默认值，作者没放出 woof 专用配置。

---

## 4. 实验结果

### 4.1 主表（ImageNet-1K 子集，官方 val 500 张）

| # | 选 mode 的方式 | 不同目标数 | **R** | **G** |
|---|---|---|---|---|
| 1 | CoDA：UMAP+HDBSCAN+三重后处理，`min_cluster_size=55` | 10 | 29.4 | **35.0** |
| 2 | 同上，`min_cluster_size=35` | 10 | 30.2 | 34.6 |
| 3 | 同上，`min_cluster_size=25` | 10 | 29.8 | 33.6 |
| 4 | **MGD³ 式朴素 K-Means**（k=IPC，原始 latent） | 10 | **32.4** | **35.4** |
| 5 | Idea 3：按簇配额，不补齐 | **3–5** | 21.0 | **24.4** |
| 6 | 随机选 10 张 | 10 | 待补 | 待补 |
| 7 | 无引导 γ=0 | — | — | 待补 |
| — | 论文 CoDA | | | **39.2±0.7** |
| — | 论文 Random / Herding / DiT | | 29.4 / 32.0 / 34.7 | |
| — | 论文 Full（全量上限） | | | 87.5 |

### 4.2 `min_cluster_size` 扫描（#1–3）：假设被证伪

原假设：该参数过大 → HDBSCAN 找不到足够簇 → 一半中心靠 k-means 从噪声点兜底 → 质量差。

参数**确实按预期起了作用**，但精度没跟着涨：

| `min_cluster_size` | 每类簇数 | k-means 兜底 | R | G |
|---|---|---|---|---|
| 55 | 3–5 | 10/10 类 | 29.4 | 35.0 |
| 35 | 4–8 | 10/10 类 | 30.2 | 34.6 |
| 25 | 5–8 | 6/10 类 | 29.8 | 33.6 |

兜底从 10 类压到 6 类，G 反而降了 1.4。**仓库默认的 55 是三档里最好的。**

### 4.3 K-Means 消融（#4）：Discovery 模块没有贡献

保持生成端完全不变，只把选 mode 换成 MGD³ 式的朴素 K-Means（无 UMAP、无 HDBSCAN、无后处理，
质心吸附到最近真实样本）：

- **G 打平**：35.4 vs 35.0（单种子下 0.4 无意义）
- **R 高 3 个点**：32.4 vs 29.4，且 32.4 恰好追平论文的 Herding(32.0)

顺带印证了论文对 K-Means 的批评在数据上确实存在——K-Means 会产生只有 1~2 个点的伪簇
（如 `[376, 290, 247, 139, 129, 112, 2, 2, 2, 1]`）——但**这些伪簇非但没有伤害，选出的样本反而更有用**。

### 4.4 Idea 3（#5）：目标"数量"才是主导因素

去掉全部补齐逻辑，只用 HDBSCAN 真正找到的 M 个模式，按簇大小分配生成配额。
UMAP/HDBSCAN 参数与 #1 完全一致（`random_state` 固定，确认是同一批簇）。

结果 **G 从 35.0 崩到 24.4**（R=21.0 在预期内，因为每类只有 3~5 张不重复的图）。

这个结果修正了 §4.3 的解读：

- G 对**用哪些** mode 不敏感（33.6 ~ 35.4）
- G 对**有几个不同的** mode 极其敏感（10 个 → 35.0，3~5 个 → 24.4）

也基本排除了"γ=0.05 太弱、引导是空转"的猜想：若引导无效，重复目标不该掉 10 个点。

**推论**：CoDA 的三重后处理不是可有可无的 hack，它是必需的——但它的真实作用远比论文声称的朴素，
不是"找到内在核心分布"，而是"凑够足够多的互不相同的锚点"。

### 4.5 数据集划分对照：不是差距的原因

| 划分 | R | G |
|---|---|---|
| ImageNet 官方（12,454 / 500） | 29.4 | 35.0 |
| fastai imagewoof2（9,025 / 3,929） | 27.9 | **30.85** |

fastai 那份**更低**，两种划分都够不到 39.2。这条线可以排除。
（两个 G 在不同验证集上评测，不能直接比大小；能比的是"都到不了 39.2"。
fastai 那版验证集 3,929 张，单种子噪声小得多，30.85 比 35.0 更可信。）

### 4.6 一条独立的诊断信号

HDBSCAN 把每类 **28%~57%（平均约 45%）的样本判成噪声**：

| 类 | 归入簇 / 总数 | 噪声占比 |
|---|---|---|
| 0 | 724 / 1300 | 44% |
| 4 | 580 / 1300 | 55% |
| 6 | 941 / 1300 | 28% |
| 8 | 562 / 1300 | 57% |

密度模型认为近一半数据不属于任何高密度区域，通常意味着**这个空间里根本没有清晰的密度结构**。
这也解释了为什么"在噪声点上跑 K-Means"的策略 2 会 10/10 类触发——噪声池永远"充足"。

---

## 5. 论文与代码的不一致

### 5.1 引导项多了一个 σ_t

论文 §3.2 推导 Δẑ₀ = γ(s_j − ẑ₀)，转到噪声空间得

    Δε_t = −γ · √(ᾱ_t/(1−ᾱ_t)) · (s_j − ẑ₀)

而 `CoDA_SDXLBasePipeline.py:265` 实际是

    pix_guide_mark = pix_diff * γ * self.scheduler.sigmas[i]      # 多出来的 σ_t
    Δε_t = −√(ᾱ_t/(1−ᾱ_t)) · pix_guide_mark

Karras 调度下 σ_t 从约 14 单调降到 0，**等于给引导强度加了一条随时间退火的曲线**：
高噪声步强、低噪声步趋近于零。这是论文里没有的设计。

### 5.2 `guideTPercent` 的语义

`CoDA_main.py` 注释给的是
`PIS = sample_step × DF × (1−GTP) + sample_step × (1−DF)`，
但代码是 `stop_idx = int(len(timesteps) × GTP)`，引导在 `t > timesteps[stop_idx]` 时生效。
timesteps 降序，所以 **GTP=0.9 表示引导开在前 22/25 步，最后 3 步关闭**。

---

## 6. 待验证的假设

### Idea 1：瓶颈是度量而非算法 ★★★

三种截然不同的聚类算法选出的 R 全落在 29.4–32.4（Random 29.4 到 Herding 32.0 的区间），
**算法换遍了结果不动**，加上 §4.6 的 45% 噪声率，都指向更底层的度量问题。

VAE latent 是 4×128×128 的空间张量，展平后欧氏距离**逐格子对比**，量的是构图相似度而非语义：
同一只狗平移几十个格子，L2 距离可能大于两个不同犬种。

做法：聚类前做空间池化消除平移敏感性。不引入外部模型，不违反 training-free 原则。

- Idea1-A：16×16 平均池化 → 4×8×8 = 256 维
- Idea1-B：每通道空间均值+方差 → 8 维（完全平移不变）

判读：R 若跳到 35+ → "DD 中 latent 聚类的失败是度量问题"；
R 若仍在 30 左右 → 更强的负面结论："VAE latent 空间不具备类别可分结构"，
所有在此做 mode discovery 的方法（CoDA/MGD³/ManifoldGD/D4M）都建在沙子上。

### Idea 2：蒸馏集要的是覆盖而非代表性 ★★★

依据：K-Means 的 1~2 点伪簇非但无害，R 还高 3 个点；且论文自己的表格显示
CoDA 在 IPC=10/20 落后、IPC≥50 才反超。低 IPC 下密度峰附近的样本彼此冗余，
离群点反而提供边界信息。

做法：k-center greedy（最远点采样）替代 K-Means。

### Idea 4：引导目标应沿时间轴由粗到细 ★★★

依据：§4.4 表明 mode 选得再好边际收益都小，真正产生增益的是引导侧（R→G 稳定 +3~5.6）。
且 §5.1 显示强度已随 t 退火，但**目标 s_j 全程固定**。

把 s_j 换成 s_j(t)，三种实例化：

- (a) 硬切换：t > τ 用父节点，否则用叶子（需层级树，可借 ManifoldGD 的二分树）
- (b) 线性插值：s_j(t) = λ_t·μ + (1−λ_t)·x_j，λ_t = √(1−ᾱ_t)（无额外超参）
- (c) **核平滑**（最有依据，且不需要树）：

      s_j(t) = Σ_k x_k w_t(k) / Σ_k w_t(k),
      w_t(k) = exp(−‖x_k − x_j‖² / (2h_t²)),   h_t² = (1−ᾱ_t)/ᾱ_t

  依据：前向过程在 t 时刻把数据分布卷积成 p_data * N(0, h_t² I)，
  对经验分布而言 t 时刻的**最优去噪器**恰好就是这个核加权均值（Tweedie / Nadaraya-Watson）。
  即"把引导目标对齐到当前噪声水平下的最优经验去噪目标"。带宽由调度器给定，无新超参。
  极限行为天然 coarse-to-fine：t→T 时趋近类均值，t→0 时趋近 x_j。

  风险：逼近经验 score 可能推向记忆训练样本，需检查生成图到最近训练图的距离分布。

**最有说服力的验证**：用 (c) 重跑 Idea 3 的配额版。若能把 24.4 拉回 34+，
就同时证明"沿轨迹分化能恢复多样性"且"不需要编造模式"，把 Idea 3 的失败转成 Idea 4 的证据。

前置的证伪实验（阶段 0）：扫 `guideTPercent` ∈ {1.0, 0.9, 0.5, 0.2} + γ=0 对照。
若四档无差别，说明时间轴无信号，Idea 4 的前提不成立。

---

## 7. 已知的工程问题

- **特征缓存膨胀 8 倍**：`get_features.py:111-118` 存的是整个 batch 张量的**视图**，
  pickle 会连底层 storage 一起存，`batch_size=8` 正好放大 8 倍（13 GB vs 理论 1.6 GB）。
  改成 `flattened_features[i].clone()` 即可。这也导致阶段 2 加载后还要 `deepcopy`，内存峰值约 26 GB。
  **跑 ImageNet-1K 时这是硬约束**：1.28M 张 × 1 MB = 1.3 TB。
- **评测的 `±0.00` 不是真方差**：`test/train.py:102` 按 GPU 数起进程、每卡一个种子，
  单卡 + `repeat=1` 只有一个样本。要真实 std 得用 `--repeat 3` 或多卡。
- **孤儿进程**：kill 驱动脚本不会杀掉已派生的生成子进程，它会继续占着 19 GB 显存导致后续任务
  连锁 OOM。后续脚本已加显存守卫（等到 `memory.used < 2000 MiB` 再启动 GPU 任务）。

## 8. 磁盘

```
results/           26 GB   两份特征缓存各 13 GB / 9 GB，蒸馏图本身只有几十 MB
model/             13 GB   SDXL base + refiner
数据                0 GB   全部软链，不占额外空间
```

特征缓存只要还想调聚类或生成参数就该留着，能省掉每次 38.5 分钟的重新编码。
